name: Auto Unzip Playlist
on:
  push:
    branches: [ main ]
    paths:
      - playlist7.json.zip
permissions:
  contents: write
jobs:
  unzip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Unzip to JSON
        run: |
          unzip -o "playlist7.json.zip" -d .
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "playlist7.json"
          git commit -m "Auto-unzip playlist7.json.zip -> playlist7.json" || echo "No changes to commit"
          git push
