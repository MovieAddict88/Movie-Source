name: Auto Unzip Playlist
on:
  push:
    branches: [ main ]
    paths:
      - playlist11.json.zip
permissions:
  contents: write
jobs:
  unzip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Unzip to JSON
        run: |
          unzip -o "playlist11.json.zip" -d .
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "playlist11.json"
          git commit -m "Auto-unzip playlist11.json.zip -> playlist11.json" || echo "No changes to commit"
          git push
